- Adopt a functionnal approach as much as possible in the project (rather than OOP) so that tests can be easier to code
- When coding tests, don't try to instanciate App, but call the functions directly